name: Publish to AUR (bin)

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: 'Existing release tag to rebuild (for manual runs)'
        required: false
        type: string
      pkgrel:
        description: 'Arch package release number for packaging-only updates'
        required: false
        default: '1'
        type: string
      commit_username:
        description: 'Git author name for AUR commits'
        required: false
        default: 'Daniel Seichter'
        type: string
      commit_email:
        description: 'Git author email for AUR commits'
        required: false
        default: 'daniel.seichter@dseichter.de'
        type: string
  workflow_call:
    inputs:
      release_tag:
        required: false
        type: string
      pkgrel:
        required: false
        default: '1'
        type: string
      commit_username:
        required: false
        default: 'Daniel Seichter'
        type: string
      commit_email:
        required: false
        default: 'daniel.seichter@dseichter.de'
        type: string
    secrets:
      AUR_SSH_PRIVATE_KEY:
        required: true

jobs:
  publish-aur:
    runs-on: ubuntu-latest
    env:
      PROJECT_NAME: Workdir
      PACKAGE_NAME: workdir-bin
      PACKAGE_BASENAME: workdir
      WORKSPACE_NAME: Workdir
      PKGBUILD_PATH: packaging/aur/PKGBUILD
    steps:
      - name: Resolve release tag
        id: vars
        env:
          EVENT_NAME: ${{ github.event_name }}
          INPUT_RELEASE_TAG: ${{ inputs.release_tag }}
        run: |
          TAG=""
          if { [ "$EVENT_NAME" = "workflow_dispatch" ] || [ "$EVENT_NAME" = "workflow_call" ]; } && [ -n "$INPUT_RELEASE_TAG" ]; then
            TAG="$INPUT_RELEASE_TAG"
          fi
          if [ "$EVENT_NAME" = "push" ] && [ "${GITHUB_REF_TYPE}" = "tag" ]; then
            TAG="${GITHUB_REF_NAME}"
          fi
          if [ -z "$TAG" ] || [[ ! "$TAG" =~ ^v[0-9A-Za-z._-]+$ ]]; then
            echo "A valid tag (e.g. v2026-03-15) is required." >&2
            exit 1
          fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"

      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ steps.vars.outputs.tag }}

      - name: Derive package version from tag
        id: version
        run: |
          TAG="${{ steps.vars.outputs.tag }}"
          VERSION="${TAG#v}"
          VERSION="${VERSION//-/.}"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Resolve package release number
        id: pkgrel
        env:
          INPUT_PKGREL: ${{ inputs.pkgrel }}
        run: |
          PKGREL="${INPUT_PKGREL:-1}"
          if [[ ! "$PKGREL" =~ ^[1-9][0-9]*$ ]]; then
            echo "A positive integer pkgrel is required." >&2
            exit 1
          fi
          echo "pkgrel=$PKGREL" >> "$GITHUB_OUTPUT"

      - name: Ensure GitHub Release exists for tag
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ steps.vars.outputs.tag }}"
          API_URL="https://api.github.com/repos/${{ github.repository }}/releases/tags/${TAG}"
          if ! curl -fsSL -H "Authorization: Bearer $GH_TOKEN" -H "Accept: application/vnd.github+json" "$API_URL" >/dev/null; then
            echo "GitHub Release for ${TAG} not found. Run release workflow first, then rerun AUR publish." >&2
            exit 1
          fi

      - name: Compute binary sha256sum
        id: checksum
        run: |
          URL="https://github.com/${{ github.repository }}/releases/download/${{ steps.vars.outputs.tag }}/${{ env.PACKAGE_BASENAME }}-archlinux-x86_64-${{ steps.vars.outputs.tag }}"
          SHA256=$(curl -fsSL "$URL" | sha256sum | cut -d' ' -f1)
          echo "sha256=$SHA256" >> "$GITHUB_OUTPUT"

      - name: Patch PKGBUILD with new version and checksum
        run: |
          sed -i "s/^pkgver=.*/pkgver=${{ steps.version.outputs.version }}/" "${{ env.PKGBUILD_PATH }}"
          sed -i "s/^pkgrel=.*/pkgrel=${{ steps.pkgrel.outputs.pkgrel }}/" "${{ env.PKGBUILD_PATH }}"
          sed -i "s/^sha256sums=.*/sha256sums=('${{ steps.checksum.outputs.sha256 }}' 'SKIP' 'SKIP')/" "${{ env.PKGBUILD_PATH }}"

      - name: Publish to AUR
        uses: KSXGitHub/github-actions-deploy-aur@abe8ac26b51011c88be58c8809fd2ac674068ea5 # v4.1.2
        with:
          pkgname: ${{ env.PACKAGE_NAME }}
          pkgbuild: ${{ env.PKGBUILD_PATH }}
          commit_username: ${{ inputs.commit_username }}
          commit_email: ${{ inputs.commit_email }}
          ssh_private_key: ${{ secrets.AUR_SSH_PRIVATE_KEY }}
          commit_message: "Update to ${{ steps.vars.outputs.tag }}-${{ steps.pkgrel.outputs.pkgrel }}"
